#ifndef GAME_PARAMS_H
#define GAME_PARAMS_H

static const int screenWidth = 1600;
static const int screenHeight = 900;

typedef enum GameScreen { MAIN_MENU=0, GAME_SCREEN} GameScreen;

#endif // !GAME_PARAMS_H
